import { SavedResultsLibrary } from "@/features/trailpack/components/SavedResultsLibrary";

export const metadata = {
  title: "Saved plans | TrailPack",
};

export default function SavedPlansPage() {
  return <SavedResultsLibrary />;
}
